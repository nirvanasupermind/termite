#include <iostream>
#include <vector>

#include "trit.h"
#include "tryte.h"
#include "word.h"
#include "cpu.h"

int main()
{
    // std::vector<termite::Word> program;
    
    // program.push_back(termite::Word(termite::Tryte("000001"), termite::Tryte("000001")));
    // // PUSH 1;
    // program.push_back(termite::Word(termite::Tryte("000001"), termite::Tryte("1TT000")));
    // // PUSH 2;
    // program.push_back(termite::Word(termite::Tryte("000011"), termite::Tryte("00001T")));
    // // SYSCALL 2;

    // termite::CPU cpu;

    while(true)
    {
        std::string inp;
        std::cout << "$ ";
        std::cin >> inp;
    }
}