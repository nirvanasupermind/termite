#ifndef COMPILER_H
#define COMPILER_H

#include <string>
#include <memory>
#include <vector>
#include "token.h"
#include "node.h"

namespace anthill {
    class Compiler {
    public:
        std::string asm;
        
    };
} // namespace anthill

#endif // COMPILER_H