int strlen(char* s) {
    int i = 0;
    while(*(s + i + i) != 0) {
        i++;
    }
    return i;
}

void* strcat(char* dst, char* a, char* b) {
    int i = 0;
    int j = 0;
    while (*(a + i) != '\0') {
        *(dst + i) = *(a + i);
        i += 2;
    }
    while (*(b + j) != '\0') {
        *(dst + i + j) = *(b + j);
        j += 2;
    }
    *(dst + i + j) = 0;
}


char* strchr(char* a, char b) {
    int i = 0;
    while (*(a + i) != b) {
        i++;
    }
    return a + i;
}

void memcpy(char*a, char* b, int n) {
    int i = 0;
    while (i < n) {
        *(b + i) = *(a + i);
        i++;        
    }
}
