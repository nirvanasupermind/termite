void print_char(char c) {
    c;
    __asm__("mov %ax,%dx\nmov $0nD0,%ax\nint $0\n");
}

void print_int(int i) {
    i;
    __asm__("mov %ax,%dx\nmov $0nDC,%ax\nint $0\n");
}

void print_str(char* s) {
    int i = 0;
    while(i < 10) {
        print_char(*(s + i));
        i++;
    }
}