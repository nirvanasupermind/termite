void putc(char c) {
    c;
    __asm__("mov %ax,%dx\nmov $0nD0,%ax\nint $0\nmov %dx,%ax\n");
}

void puti(int i) {
    i;
    __asm__("mov %ax,%dx\nmov $0nDC,%ax\nint $0\nmov %dx,%ax\n");
}

void puts(char* s) {
    int i = 0;
    while(i < 10) {
        putc(*(s + i));
        i = i + 1;
    }
}