#if !defined(UTIL_HPP)
#define UTIL_HPP

namespace termite {
    enum FromBCT { from_bct };
    enum FromVal { from_val };
    enum FromTrits { from_trits };
} // namespace termite


#endif // UTIL_HPP