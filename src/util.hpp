#if !defined(UTIL_HPP)
#define UTIL_HPP

#include <string>

namespace termite
{    
    // Dummy enums used as constructor labels

    enum FromBCT { from_bct };

    enum FromInt { from_int };
} // namespace termite

#endif // UTIL_HPP
