#if !defined(UTIL_HPP)
#define UTIL_HPP

namespace termite
{
    // Dummy enums used as constructor labels

    enum FromBCT { from_bct };

    enum FromVal { from_val };
} // namespace termite

#endif // UTIL_HPP
